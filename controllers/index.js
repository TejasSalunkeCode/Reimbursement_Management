'use strict';

/**
 * Controllers barrel.
 * Export all controller modules from here so routes can import cleanly.
 *
 * Example:
 *   const { userController } = require('../controllers');
 */

// module.exports.userController = require('./user.controller');
module.exports = {};
