'use strict';

/**
 * Services barrel.
 * Business logic lives here, separate from route/controller concerns.
 *
 * Example:
 *   const { userService } = require('../services');
 */

// module.exports.userService = require('./user.service');
module.exports = {};
